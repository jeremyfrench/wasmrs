[package]
name = "wasm_stats"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
approx = "0.5"
wasm-bindgen = "0.2.73"

[profile.release]
lto = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
